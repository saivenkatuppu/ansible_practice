- name: nginx install and run
  host: frontend
  become: yes
  tasks:
  - name: install nginx server
    ansible.builtin.package:
    name: nginx
    state: present

  - name:  nginx service
    ansible.builtin.service:
     name: nginx
     state: started
     enabled: yes
   